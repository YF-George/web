# 公開設定（會出現在 client 端）
PUBLIC_API_URL=https://api.example.com

# 私密設定（僅 server 端可見）
# 本地開發時複製為 .env.local，並填入實際值

# Anonymous Form System: Salt for pseudonym hashing (REQUIRED)
# Generate: openssl rand -base64 32 (Linux/Mac) or use PowerShell crypto
PSEUDONYM_SALT=your-secure-random-salt-here

# DATABASE_PASSWORD=your_db_password
# JWT_SECRET=your_jwt_secret
# API_KEY=your_api_key

# === 即時通道（Ably）與 KV 設定 ===
# 前端 Realtime 使用；正式環境建議改為 Token Auth，避免在 client 端暴露金鑰
PUBLIC_ABLY_KEY=your-ably-key:xxxxxxxxxxxxxxxxxxxxxxxx

# 後端 REST 發布使用（僅 server 端）
ABLY_API_KEY=your-ably-key:xxxxxxxxxxxxxxxxxxxxxxxx

# Vercel KV（用於共享團隊資料持久化）
KV_REST_API_URL=https://YOUR-KV-URL
KV_REST_API_TOKEN=YOUR-KV-TOKEN
