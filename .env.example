# 公開設定（會出現在 client 端）
PUBLIC_API_URL=https://api.example.com

# 私密設定（僅 server 端可見）
# 本地開發時複製為 .env.local，並填入實際值

# Anonymous Form System: Salt for pseudonym hashing (REQUIRED)
# Generate: openssl rand -base64 32 (Linux/Mac) or use PowerShell crypto
PSEUDONYM_SALT=your-secure-random-salt-here

# DATABASE_PASSWORD=your_db_password
# JWT_SECRET=your_jwt_secret
# API_KEY=your_api_key

# ----- Liveblocks 範例設定 -----
# 在本機建立 .env 或 .env.local 並填入實際的 secret 與（可選）公開金鑰
# 注意：不要將實際的 LIVEBLOCKS_SECRET_KEY 推上遠端或放在前端程式碼中。
LIVEBLOCKS_SECRET_KEY=

# 若需要在 client 端使用 Liveblocks 的公開金鑰，可填入（可選）
VITE_LIVEBLOCKS_PUBLIC_KEY=

# 覆寫 auth endpoint（預設為 /api/liveblocks/auth）
VITE_LIVEBLOCKS_AUTH_ENDPOINT=/api/liveblocks/auth
